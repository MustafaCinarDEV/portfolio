const { resolve } = require("path")

module.exports = {
  darkMode: "class", // or 'media' or 'class'
  theme: {
    extend: {},
  },
  theme: {
    fontFamily: {
      sans: ["Inter", "sans-serif"],
    },
  },
}